if SHIELD_CROSSES_LEFT || SHIELD_CROSSES_RIGHT

config ZMK_KEYBOARD_NAME
	default "ZMK-New"

config ZMK_SPLIT
	default y

# 左手のボールを動かすために必須の設定
config ZMK_MOUSE
	default y

config ZMK_POINTING
	default y

endif
