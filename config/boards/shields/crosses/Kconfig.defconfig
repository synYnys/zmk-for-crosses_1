# LEFT のところを n (オフ) に書き換えてください
if SHIELD_CROSSES_LEFT

config ZMK_KEYBOARD_NAME
	default "ZMK-New"

config ZMK_SPLIT_BLE_ROLE_CENTRAL
	default n

endif

if SHIELD_CROSSES_LEFT || SHIELD_CROSSES_RIGHT

config ZMK_SPLIT
	default y

endif
