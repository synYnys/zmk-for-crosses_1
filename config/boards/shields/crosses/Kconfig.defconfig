if SHIELD_CROSSES_LEFT || SHIELD_CROSSES_RIGHT

config ZMK_KEYBOARD_NAME
	default "ZMK-New"

config ZMK_SPLIT
	default y

# 左右共通でマウス機能を有効化
config ZMK_MOUSE
	default y

config ZMK_POINTING
	default y

endif

# 右手だけをリーダーとして明示（左手側の n 設定は削除してください）
if SHIELD_CROSSES_RIGHT
config ZMK_SPLIT_BLE_ROLE_CENTRAL
	default y
endif
